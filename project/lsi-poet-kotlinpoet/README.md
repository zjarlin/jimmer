# lsi-poet-kotlinpoet

`lsi-poet-kotlinpoet` 是 `lsi-poet` 到 KotlinPoet 的边界适配器。KotlinPoet 类型只存在于适配器内部，公共 renderer 接口只接收 LSI Poet 模型并返回 `GeneratedArtifact`。

坐标：`org.babyfish.jimmer:lsi-poet-kotlinpoet`
